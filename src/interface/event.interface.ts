/**
 * @interface
 * Helper interface used for definition in TypedEventEmitter.
 * Functionality that supports events via the {@link TypedEventEmitter} should implement
 * their own interface which extends this interface.
 */
export interface IEvent {}